# CLAUDE.md — Game Zone v3

## المشروع
Game Zone (قيم زون) — منصة ألعاب جماعية حقيقية (ملتي بلاير). أصدقاء يدخلون غرفة بكود ويلعبون مع بعض.

## التشغيل
```bash
npm install
cd server && npm install && cd ..
npm run dev:all
# الكلاينت: http://localhost:3000
# السيرفر: http://localhost:3001
```

## التقنيات
- **Frontend:** React 18 + Vite 6, CSS-in-JS inline styles, RTL عربي
- **Backend:** Express + Socket.IO (port 3001), in-memory storage
- **بدون مكتبات UI خارجية** — كل شي inline styles
- تصميم موبايل أول (max-width: 480px)

## هيكل المشروع
```
├── server/
│   ├── index.js              ← Express + Socket.IO server
│   ├── roomManager.js        ← إدارة الغرف (إنشاء/انضمام/طرد)
│   ├── sessionManager.js     ← جلسات اللاعبين (UUID tokens)
│   ├── utils.js              ← shuffle, pick, genCode
│   ├── games/
│   │   ├── pyramid.js        ← محرك الهرم (سيرفر)
│   │   └── arena.js          ← محرك الحلبة (سيرفر)
│   └── data/
│       ├── questions.js       ← بنك أسئلة الهرم (100+ سؤال) + إعدادات الصعوبة
│       └── challenges.js      ← بيانات تحديات الحلبة
├── src/
│   ├── App.jsx               ← Router + state management
│   ├── main.jsx              ← نقطة الدخول
│   ├── socket.js             ← Socket.IO client singleton
│   ├── session.js            ← localStorage helpers
│   ├── theme.js              ← ألوان + CSS animations
│   ├── hooks/
│   │   ├── useSocket.js      ← hook للاشتراك بأحداث السوكت
│   │   └── useSession.js     ← hook للجلسة
│   ├── components/
│   │   ├── ui/ (Btn, Card, Badge, Timer, Confetti)
│   │   ├── PlayerAvatar.jsx
│   │   └── PlayerGrid.jsx
│   └── screens/
│       ├── IdentitySetup.jsx  ← اختيار اسم + أفاتار
│       ├── Landing.jsx        ← إنشاء/دخول غرفة
│       ├── Lobby.jsx          ← غرفة الانتظار
│       ├── Dashboard.jsx      ← نتائج + ترتيب
│       ├── pyramid/           ← شاشات الهرم
│       └── arena/             ← شاشات الحلبة
```

## تدفق الشاشات
```
IdentitySetup → Landing → Lobby → PyramidGame/ArenaGame → Dashboard → Lobby
```

## الألعاب

### 🔺 الهرم (Pyramid)
- كويز باتل رويال — 2-20 لاعب
- نظام راوندات: كل سؤال = راوند، إقصاء فوري بعد كل سؤال
- عدد الأسئلة ديناميكي حسب عدد اللاعبين (5-15 سؤال)
- تدرج الصعوبة: easy → medium → hard → extreme
- 100+ سؤال عشوائي، كل شي يُحسب على السيرفر
- خطوط نجاة: تخطي، 50/50، +8 ثواني (شخصية لكل لاعب)
- اللي يغلط أو ما يجاوب يطلع فوراً، لو كلهم صح الأبطأ يطلع

### ⚔️ الحلبة (Arena)
- 6 تحديات مختلفة: سرعة ضغط، ذاكرة، رياضيات، ردة فعل، ترتيب حروف، ألوان
- 5 جولات عشوائية، ~30% إقصاء كل جولة
- كل اللاعبين يحصلون نفس التحدي، النتائج تُحسب على السيرفر

## نظام الغرف
- كود 5 أحرف (ABCDEFGHJKLMNPQRSTUVWXYZ23456789)
- 2-20 لاعب
- الهوست يختار اللعبة ويبدأها
- نقل تلقائي للهوست إذا طلع
- إعادة اتصال تلقائية (Socket.IO reconnection)

## Socket Events الرئيسية
- `room:create/join/leave/kick/start-game`
- `pyramid:start/countdown/round/answer/lifeline/round-result/champion`
- `arena:start/countdown/round-intro/challenge/submit/reaction-go/round-result/champion`

## نظام الألوان
- Primary: #00E676 (أخضر)
- Gold: #FFD700, Red: #FF4444, Purple: #9B59B6
- خلفية: #060b0a (أسود غامق)

## ملاحظات
- الأسئلة والإجابات على السيرفر فقط (منع الغش)
- التايمر: السيرفر يرسل `timerEnd` timestamp، الكلاينت يحسب محلياً
- كل حساب نقاط ونظام إقصاء على السيرفر — الكلاينت عرض فقط
